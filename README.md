# Random Quote Generator 💬
## ✨ Features

* **Randomized Display:** Selects and shows a new quote randomly from a list of 50 top quotes.
* **Dynamic UI:** Uses React state (`useState`) to update the quote without a full page reload.
* **Simple Styling:** Styled using basic CSS and utility classes (assuming Tailwind or similar utility framework based on the class names used).
* ## 🛠️ Technologies Used

* **React:** For building the component-based user interface.
* **JavaScript (ES6+):** For the random selection logic.
* **HTML/CSS:** For structure and presentation.
* **Vite / Create React App (Likely):** For the project build environment.
